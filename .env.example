# ─────────────────────────────────────
# Integration Service
# ─────────────────────────────────────
PORT=3001
HOST=0.0.0.0
JWT_SECRET=super-secret-key-change-me

# ─────────────────────────────────────
# Actual Budget Configuration
# When using docker-compose, ACTUAL_SERVER_URL is overridden
# to http://actual-server:5006 automatically.
# ─────────────────────────────────────
ACTUAL_SERVER_URL=http://actual-server:5006
# Support also: ACTUAL_BASE_URL (alternative name, e.g. docker-compose production)
ACTUAL_PASSWORD=your-actual-server-password
ACTUAL_SYNC_ID=your-budget-sync-id
ACTUAL_DATA_DIR=./budget-data
# Optional: E2EE passphrase (if budget is encrypted)
# ACTUAL_E2EE_PASSWORD=your-e2ee-passphrase

# ─────────────────────────────────────
# Docker Compose (optional overrides)
# ─────────────────────────────────────
# ACTUAL_PORT=5006
